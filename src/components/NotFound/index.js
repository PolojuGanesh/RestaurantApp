import './index.css'

const NotFound = () => (
  <div className="not-found-container">
    <h1 className="not-found-heading">Page Not Found</h1>
    <p className="not-found-para">
      It looks like the page you're trying to reach is not available.
    </p>
  </div>
)

export default NotFound
